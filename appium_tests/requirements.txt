# Appium E2E Testing — Nutrixa Android App
# Install with: python -m pip install -r requirements.txt

# Appium Python client (official)
Appium-Python-Client==3.1.0

# Test framework
pytest==8.2.2
pytest-html==4.1.1

# Excel report generation
openpyxl==3.1.5

# NOTE: Pillow removed — not needed. Appium's save_screenshot() saves PNG natively.
# Pillow 10.x has no pre-built wheel for Python 3.14+.

# Utility
python-dateutil==2.9.0
colorama==0.4.6
